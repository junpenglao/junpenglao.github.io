_site
.sass-cache
*.html~
.gitignore~
.ipynb_checkpoints
.DS_Store
cv/papers/_temp/*

# Astro build artifacts
node_modules/
dist/
.astro/

# Local-only agent guidance — CONFIDENTIAL, must never be committed (public repo).
CLAUDE.md

# CONFIDENTIAL site data — unannounced/flagged items (e.g. src/data/work.secret.ts).
*.secret.ts

# Private writing notes / idea parking lot — local only, never published or committed.
ideas/

# Private personal-data imports (e.g. the Twitter/X archive for the voice-mining
# pipeline) and all derived corpora/outputs — contains PII/DMs, never commit.
private/

# Voice system is local-only: the /voice review skill never ships (the voice
# profiles it reads already live under gitignored private/).
.claude/skills/